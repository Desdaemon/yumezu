/target
/dist
/data.json
/tools/atlas/target
# Downloaded wiki images, and the atlas packed out of them: both derived, both rebuilt by
# `just thumbnails`, and the wiki's to distribute rather than this repository's.
/tools/atlas/cache
/static/thumbnails.jpg
# The key `android/build.sh` signs release apks with. Never committed: whoever holds it can ship
# an update that Android will accept as this app. Back it up somewhere other than this directory.
/android/production.jks
